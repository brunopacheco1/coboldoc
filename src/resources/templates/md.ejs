# <%= fileName %>
<%= !!fileDescription ? `\n${fileDescription}\n` : '' %>
author: <%= author %>

License: <%= license %>
<% if(modules.length > 0) { %>
## Module Summary

| Name | Description |
| ----------- | ----------- | <% modules.forEach((module) => { %>
| [<%= module.name %>](#<%= module.name %>) | <%= module.description %> | <% }); %>
<% } if(functions.length > 0) { %>
## Function Summary

| Name | Description |
| ----------- | ----------- | <% functions.forEach((cobolFunction) => { %>
| [<%= cobolFunction.name %>](#<%= cobolFunction.name %>) | <%= cobolFunction.description %> | <% }); %>
<% } if(modules.length > 0) { %>
## Module Details
<% modules.forEach((module) => { %>
### <%= module.name %>

<%= module.description %>
<% }); } if(functions.length > 0) { %>
## Function Details
<% functions.forEach((cobolFunction) => { %>
### <%= cobolFunction.name %>

*<%= !!cobolFunction.return.type ? `{${cobolFunction.return.type}} ` : '' %><%= cobolFunction.name %>(<%= cobolFunction.params.map(p => p.name).join(', ') %>)*

<%= cobolFunction.description %>

#### Parameters
<% cobolFunction.params.forEach((param) => { %>
> <%= !!param.type ? `*{${param.type}}* ` : '' %>**<%= param.name %>** <%= param.description %> <% }); %>

#### Returns

> <%= !!cobolFunction.return.type ? `*{${cobolFunction.return.type}}* ` : '' %><%= cobolFunction.return.description %>
<% }); } %>